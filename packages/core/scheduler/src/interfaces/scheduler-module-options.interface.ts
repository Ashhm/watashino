import { RegisterQueueOptions } from '@nestjs/bullmq/dist/interfaces/register-queue-options.interface';

/**
 * Options for configuring the SchedulerModule
 */
export type SchedulerModuleOptions = RegisterQueueOptions
